A batch file that takes info from target computer, replaces the computer's background, and puts 500 shortcuts with a given image (loaded from an ftp server) onto their desktop. 
Info grabbed includes the ip address and the names of all files under the user directory.
Info is then transfered to an ftp server.
